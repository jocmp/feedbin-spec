meta {
  name: POST -pages-json
  type: http
  seq: 2
}

post {
  url: {{ _.base_url }}/pages.json
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "url": "string",
    "": "string"
  }
}
